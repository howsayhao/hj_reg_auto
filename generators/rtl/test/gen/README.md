# gen (generated rtl modules)

consists of generated rtl modules and header files based on input .xlsx/.rdl files.

- `regmst` module top files

- `regslv` module top files

- `regmst` header file

- `regslv` header file
